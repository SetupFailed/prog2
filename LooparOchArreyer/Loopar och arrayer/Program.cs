


string[] toys = { "Bakugan", "Beyblade", "Omnitrix", "Tamagotchi", "HotWheels" };
string[] people = { "Sunnie", "Tommi", "Tin", "Zacke", "Nicole" };

foreach (string i in toys)
{
    Console.WriteLine(i);
}

/*for (int i = 0; i < toys.Length; i++)
{
    Console.WriteLine(people[i] + " har " + toys[i]);
}*/

int[] num = {1, 3, 5, 7, 9};

for (int i = 0; i < toys.Length; i++) {
    
}